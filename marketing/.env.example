# The app deployment (auth, dashboard, booking). The marketing CTAs
# ("Create your page", "Log in") point here. Until the app is deployed these
# links won't resolve — that's fine for a marketing-first launch.
#
# Local:      NEXT_PUBLIC_APP_URL="http://app.localhost:3000"
# Production: NEXT_PUBLIC_APP_URL="https://app.reservme.pro"
NEXT_PUBLIC_APP_URL="https://app.reservme.pro"
